# Exercice : Intégration de l'interface Google

## Présentation
Projet réalisé dans le cadre d'un exercice de cours. L'objectif était de reproduire la page d'accueil de Google Chrome en utilisant uniquement HTML5 et CSS3.

## Objectifs techniques
* **Structure HTML** : Utilisation de balises sémantiques (`header`, `main`, `footer`).
* **Flexbox** : Centrage des éléments et gestion des alignements verticaux/horizontaux.
* **Modèle de boîte** : Contrôle des dimensions (`width`, `height`) et des espacements.
* **Interactivité** : Gestion des états au survol (`:hover`) sur les liens et boutons.
* **Positionnement** : Utilisation du mode `fixed` pour le bouton de personnalisation.

## Défis résolus
* Correction de l'étirement des blocs via `width: fit-content`.
* Création de formes circulaires avec `border-radius: 50%`.
* Nettoyage des styles par défaut du navigateur (liens, inputs).
